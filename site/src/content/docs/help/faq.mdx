---
title: FAQ
description: Answers to common questions about stint.
---

import JsonLd from "../../../components/JsonLd.astro";

export const faqPage = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: [
    {
      "@type": "Question",
      name: "Why macOS only?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "The shell is Tauri 2 (cross-platform in principle), but stint leans hard on macOS-specific bits: Keychain for secret storage, menu-bar tray, dock visibility transitions, native macOS menus. Porting to Linux or Windows would mean either dropping those features or building per-platform equivalents. No fundamental blocker — just a scope decision.",
      },
    },
    {
      "@type": "Question",
      name: "Does stint work offline?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Yes, fully. Every mutation persists to a local SQLite database immediately. When you go online, the sync queue drains and Solidtime catches up. The popover and main window work normally offline; calendar refresh and Solidtime sync queue and retry transparently when connectivity returns.",
      },
    },
    {
      "@type": "Question",
      name: "Can I use stint without Solidtime?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Not currently. stint is designed around Solidtime as the source of truth for projects, organizations, and historical entries. A standalone mode is on the roadmap but unscheduled.",
      },
    },
    {
      "@type": "Question",
      name: "How is my data stored?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Locally only. SQLite at ~/Library/Application Support/stint/stint.db; secrets in the macOS Keychain under tech.reyem.stint.*; logs at ~/Library/Logs/tech.reyem.stint/. No cloud storage, no third-party backup.",
      },
    },
    {
      "@type": "Question",
      name: "Does stint phone home?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "No analytics, no telemetry. The only outbound traffic stint generates is to your Solidtime instance, your calendar provider if connected, and a static JSON update manifest on github.com once every 24 hours. You can disable the auto-update check in Settings.",
      },
    },
    {
      "@type": "Question",
      name: "Where does the CLI binary live after brew install?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "On Apple Silicon at /opt/homebrew/bin/stint, on Intel Macs at /usr/local/bin/stint. Both are symlinks into /Applications/Stint.app/Contents/MacOS/stint — the same binary that ships via curl|sh.",
      },
    },
    {
      "@type": "Question",
      name: "Can I use brew install and curl|sh together?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Yes — they coexist at different paths. Brew symlinks to /opt/homebrew/bin/stint; the standalone installer writes to /usr/local/bin/stint or ~/.local/bin/stint. Whichever is earlier in your PATH wins. stint update on the brew-installed binary detects the .app-bundled install and tells you to use brew upgrade --cask stint instead.",
      },
    },
    {
      "@type": "Question",
      name: "How do I update stint?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Three paths, each works: in-app (Settings → Updates → Install, auto-checked every 24h); brew (brew upgrade --cask stint); standalone CLI (stint update). All three converge on the same release artifacts.",
      },
    },
    {
      "@type": "Question",
      name: "Can I trigger stint from a shell script, Raycast, or Alfred?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Yes. Every CLI verb accepts a global --json flag for structured output (stint --json current, stint --json start \"writing tests\", etc.). For deep links from Raycast/Alfred/Shortcuts, use the stint:// URL scheme (stint://start?description=…, stint://stop, stint://current, stint://entry/<uuid>). See the Scripting and CLI reference pages.",
      },
    },
    {
      "@type": "Question",
      name: "How do I drive stint from Claude, Codex, or OpenCode?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Run stint skill install <claude|codex|opencode> (or omit the harness for an interactive picker). It registers stint's MCP server with the harness and installs a SKILL.md / rules fragment teaching the agent when and how to call the eight tools. See the AI integration page.",
      },
    },
    {
      "@type": "Question",
      name: "Is the local HTTP API a privacy concern?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "It's off by default. When you enable it (stint config set api.enabled true), the server binds to 127.0.0.1 on an ephemeral port — loopback only, never reachable from another machine. It only runs while Stint.app is open; quitting the app closes the listener.",
      },
    },
    {
      "@type": "Question",
      name: "Can I deep-link into stint?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Yes. Stint.app registers the stint:// URL scheme on macOS. Supported actions: stint://start?description=…&project=…&billable=true, stint://stop, stint://current, stint://entry/<local-uuid>. Use open from the shell or wire them into Raycast / Alfred / Shortcuts.",
      },
    },
  ],
};

<JsonLd data={faqPage} />

## Why macOS only?

The shell is Tauri 2 (cross-platform in principle), but stint leans hard
on macOS-specific bits: Keychain for secret storage, menu-bar tray, dock
visibility transitions, native macOS menus. Porting to Linux or Windows
would mean either dropping those features or building per-platform
equivalents (`secret-service` on Linux, `wincred` on Windows, etc.).

No fundamental blocker — just a scope decision. A Linux build is
plausibly Phase 8+ work if there's demand.

## Does stint work offline?

Yes, fully. Every mutation persists to a local SQLite database
immediately. When you go online, the sync queue drains and Solidtime
catches up.

The popover and main window work normally offline. The only thing that
fails is calendar refresh (Google API call) and Solidtime sync — both
queue and retry transparently when connectivity returns.

## Can I use stint without Solidtime?

Not currently. stint is designed around Solidtime as the source of truth
for projects, organizations, and historical entries. A standalone mode
(local-only, no remote sync) is on the roadmap but unscheduled.

If you want a self-hosted backend without standing up Solidtime,
[Solidtime](https://www.solidtime.io) has a Docker setup that runs on
~512MB of RAM.

## How is my data stored?

| Where | What |
|---|---|
| `~/Library/Application Support/stint/stint.db` | SQLite database — time entries, projects, calendar accounts, sync queue |
| macOS Keychain (`tech.reyem.stint.*` service prefix) | Solidtime PAT/OAuth tokens, calendar OAuth tokens (per account) |
| `~/Library/Logs/tech.reyem.stint/stint.log.<date>` | Application logs, daily rotation, 14-file cap |

Everything is local. No cloud storage, no third-party backup.

## Does stint phone home?

No analytics, no telemetry. The only outbound traffic stint generates is:

1. **Solidtime API calls** — to your Solidtime instance, for sync.
2. **Calendar API calls** — to Google (if you connected a calendar).
3. **Auto-update check** — once every 24 hours, to
   `github.com/reyemtech/stint/releases/latest/download/latest.json`
   (a static JSON manifest, no user identifier).
4. **`stint update`** for standalone CLI installs — same endpoint, on
   demand.

You can disable auto-update under Settings → Updates (sets the polling
interval effectively to "never until you click Check now").

## Where does the CLI binary live after `brew install`?

Apple Silicon: `/opt/homebrew/bin/stint` → symlink into
`/Applications/Stint.app/Contents/MacOS/stint`.

Intel Mac: `/usr/local/bin/stint` → same symlink target.

The CLI binary inside the .app bundle is the same one shipped via
`curl | sh`; the only difference is the install path.

## Can I use `brew install` + `curl | sh` together?

Yes — they coexist at different paths. `brew install` symlinks to
`/opt/homebrew/bin/stint`; the standalone installer writes to
`/usr/local/bin/stint` or `~/.local/bin/stint`. Whichever is earlier in
your `PATH` wins when you type `stint`.

`stint update` on the brew-installed binary won't try to overwrite the
brew symlink; it detects the `.app`-bundled install and tells you to use
`brew upgrade --cask stint` instead.

## How do I update stint?

Three paths, each works:

1. **In-app**: Settings → Updates → Install. Auto-checked every 24h.
2. **Brew**: `brew upgrade --cask stint`. Picks up the latest cask version.
3. **Standalone CLI**: `stint update`. Self-replaces the binary atomically.

All three converge on the same release artifacts.

## Can I trigger stint from a shell script, Raycast, or Alfred?

Yes. Every CLI verb accepts a global `--json` flag for structured
output:

```bash
stint --json current
stint --json start "writing tests" --project p-uuid
stint --json today | jq '.[] | .description'
```

For deep links from Raycast / Alfred / Shortcuts, use the `stint://`
URL scheme:

```bash
open "stint://start?description=writing%20tests"
open "stint://stop"
open "stint://current"
```

See [Scripting stint](/scripting/) and the
[CLI reference](/reference/cli/#stint-url-scheme).

## How do I drive stint from Claude, Codex, or OpenCode?

```bash
stint skill install              # interactive picker
stint skill install claude       # explicit
```

The installer registers stint's MCP server with the harness and lays
down a `SKILL.md` / rules fragment so the agent knows when and how to
call the eight tools (`start`, `stop`, `current`, `list_entries`,
`list_projects`, `list_tasks`, `update_entry`, `delete_entry`). See
[AI integration](/ai-integration/) for the full integration story.

## Is the local HTTP API a privacy concern?

The HTTP API is **off by default**. When you enable it
(`stint config set api.enabled true`), the server binds to
`127.0.0.1` on an ephemeral port — loopback only, never reachable from
another machine on the network. It runs inside the Stint.app process,
so quitting the app closes the listener. There's no separate daemon
and no token (localhost is the trust boundary).

## Can I deep-link into stint?

Yes. Stint.app registers the `stint://` URL scheme on macOS:

| URL | Effect |
|---|---|
| `stint://start?description=…&project=…&billable=true` | Start a timer |
| `stint://stop` | Stop the running timer |
| `stint://current` | Focus the current-entry view |
| `stint://entry/<local-uuid>` | Open an entry in the main window |

`open "stint://stop"` from the shell, or wire into Raycast / Alfred /
Shortcuts.

## Where's the source?

[github.com/reyemtech/stint](https://github.com/reyemtech/stint).

[Issues](https://github.com/reyemtech/stint/issues) for bug reports and
feature requests.

## Who builds stint?

[Reyem Tech](https://www.reyem.tech). stint is MIT-licensed — fork it,
modify it, ship your own version with a different OAuth client.

The repository's `CLAUDE.md` is the canonical reference for repo
structure, conventions, and gotchas — useful reading even if you're not
contributing.
