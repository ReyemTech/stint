---
// Compact CLI code block — used in the "two surfaces" split.
---

<pre class="code-block"><code><span class="t-prompt">$</span> stint start <span class="t-str">"fixing the queue bug"</span>
<span class="t-ok">✓</span> tracking · 0s

<span class="t-prompt">$</span> stint stop
<span class="t-ok">✓</span> stopped · 1h 12m · synced</code></pre>
