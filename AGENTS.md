# AGENTS.md

This repository documents its agent-facing instructions in [`CLAUDE.md`](./CLAUDE.md).

All conventions, build commands, repository layout, phase status, and gotchas
live there. If you are not Claude — read that file first; nothing in it is
Claude-specific.

When editing either file, keep both in sync. The simplest pattern is to make
substantive edits in `CLAUDE.md` and leave this file as a pointer.
