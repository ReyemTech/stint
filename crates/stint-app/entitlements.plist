<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
  <!-- keychain-access-groups removed: the $(AppIdentifierPrefix) template is
       an Xcode-only substitution that codesign doesn't process, so the
       literal string ends up in the binary's entitlements and macOS kernel
       rejects launch with "Taskgated Invalid Signature". Non-sandboxed
       CLI + GUI share Keychain entries via the standard one-prompt-per-binary
       ACL flow; the entitlement isn't required for that to work. Phase 4.5
       will revisit when App Sandbox lands for MAS. -->
</dict>
</plist>
